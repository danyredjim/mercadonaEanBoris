apiVersion: v1
kind: Service
metadata:
  name: mercadona-eanboris-service
spec:
  type: NodePort
  selector:
    app: mercadona-eanboris
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007